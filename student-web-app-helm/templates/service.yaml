apiVersion: v1
kind: Service
metadata:
  name: {{ include "student-web-app.fullname" . }}
  labels:
    {{- include "student-web-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "student-web-app.selectorLabels" . | nindent 4 }}
